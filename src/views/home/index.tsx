import { defineComponent } from "vue";

export default defineComponent({
  render() {
    return (
      <div class={`w-full h-full`}>
        <h1 class={`text-zinc-900 dark:text-yellow-50`}>Home</h1>
      </div>
    );
  }
});
