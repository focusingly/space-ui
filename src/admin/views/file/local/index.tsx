import { defineComponent } from "vue";

export const LocalFileManager = defineComponent({
  name: "LocalFileManager",
  setup() {
    return () => <div></div>;
  }
});

export default LocalFileManager;
